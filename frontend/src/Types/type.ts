export type EndPoints = {
  id: number;
  endPoint: string;
  linkName: string;
  key?: string;
};

export type TestimonialData = {
  name: string;
  content: string;
  rating: number;
  imageUrl: string;
};
